# ═══════════════════════════════════════════════
#  SD Voice AI Service — Environment Config
# ═══════════════════════════════════════════════
#  Copy this file to .env.local and fill in values.
#  NEVER commit .env.local to version control.
# ═══════════════════════════════════════════════

# ── Environment ──
ENVIRONMENT=development          # development | staging | production

# ── LiveKit ──
LIVEKIT_URL=wss://your-project.livekit.cloud
LIVEKIT_API_KEY=your-livekit-api-key
LIVEKIT_API_SECRET=your-livekit-api-secret

# ── Twilio SIP Trunk ──
SIP_OUTBOUND_TRUNK_ID=your-sip-trunk-id

# ── Deepgram (STT) ──
DEEPGRAM_API_KEY=your-deepgram-api-key

# ── ElevenLabs (TTS) ──
ELEVENLABS_API_KEY=your-elevenlabs-api-key

# ── OpenAI (LLM — primary) ──
OPENAI_API_KEY=your-openai-api-key

# ── Anthropic (LLM — fallback, optional) ──
ANTHROPIC_API_KEY=your-anthropic-api-key

# ── Supabase ──
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your-supabase-service-role-key

# ── Redis ──
REDIS_URL=redis://localhost:6379/0

# ── Caller Identity ──
CALLER_ID_NUMBER=+15551234567    # Your Twilio phone number
CLINIC_NAME="Referral Network"   # Displayed in agent greeting

# ── Feature Flags ──
FEATURE_HUMAN_REVIEW=true        # Require human review for low-confidence updates
FEATURE_CALL_RECORDING=true      # Enable call recording via LiveKit
FEATURE_FALLBACK_TTS=true        # Fall back to Deepgram TTS if ElevenLabs fails

# ── Operational Limits ──
MAX_CONCURRENT_CALLS=10
MAX_CALL_DURATION_SECONDS=300    # 5-minute hard limit per call
MAX_RETRY_ATTEMPTS=3

# ── Logging ──
LOG_LEVEL=INFO                   # DEBUG | INFO | WARNING | ERROR
